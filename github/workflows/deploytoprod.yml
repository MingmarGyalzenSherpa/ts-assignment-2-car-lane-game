name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull-request:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm Install

      - name: Build Project
        run: npm run build

      - name: Deploy to netlify
        run: netlify deploy --site "${{secrets.CAR_TOKEN}}" --auth "${{secrets.SITE_ID}}" --dir "dist"  --prod
