export enum CarType {
  player = "player",
  obstacle = "obstacle",
}

export enum GameState {
  Waiting = "WAITING",
  Running = "RUNNING",
  Paused = "PAUSED",
  End = "END",
}

export enum Direction {
  Left = "LEFT",
  Right = "RIGHT",
}
